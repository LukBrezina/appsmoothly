[Unit]
Description=Keep the appsmoothly workspace current

[Timer]
# Hourly. The workspace only changes when something is pushed to the repo, so
# checking every 15 minutes was pointless churn.
OnBootSec=5min
OnUnitActiveSec=1h
AccuracySec=5min
Persistent=true

[Install]
WantedBy=timers.target

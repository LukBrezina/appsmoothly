# secret.<project>... -> the bot bridge, which serves the one-time paste form.
# The bridge is already running; nothing new is needed to host this.
@secret header_regexp Host ^secret\.
handle @secret {
	reverse_proxy 172.17.0.1:4488
}
